<Window x:Class="TradeDemo.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:TradeDemo.Wpf.Controls"
        mc:Ignorable="d"
        Title="Trade Signal Terminal (WPF)"
        Height="980"
        Width="1760"
        MinHeight="860"
        MinWidth="1380"
        Background="#0a0e17">
    <Window.Resources>
        <SolidColorBrush x:Key="BgPrimary" Color="#0a0e17"/>
        <SolidColorBrush x:Key="BgSecondary" Color="#111827"/>
        <SolidColorBrush x:Key="BgPanel" Color="#1a1f2e"/>
        <SolidColorBrush x:Key="BgPanelAlt" Color="#0f172a"/>
        <SolidColorBrush x:Key="BorderBrushPanel" Color="#2a3142"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#e2e8f0"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748b"/>
        <SolidColorBrush x:Key="CyanBrush" Color="#06b6d4"/>
        <SolidColorBrush x:Key="GreenBrush" Color="#10b981"/>
        <SolidColorBrush x:Key="RedBrush" Color="#ef4444"/>
        <SolidColorBrush x:Key="YellowBrush" Color="#f59e0b"/>
        <SolidColorBrush x:Key="GridLineBrush" Color="#273244"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas"/>
        </Style>

        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="7"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushPanel}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="4"/>
        </Style>

        <Style x:Key="TerminalButton" TargetType="Button">
            <Setter Property="Background" Value="#172554"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="BuyOrderButton" TargetType="Button">
            <Setter Property="Background" Value="#00c853"/>
            <Setter Property="Foreground" Value="#04130a"/>
            <Setter Property="BorderBrush" Value="#21e67a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18,13"/>
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#00b84d"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#3cff91"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#009f43"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PanelTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="TerminalGridBase" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="None"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowBackground" Value="#141b2b"/>
            <Setter Property="AlternatingRowBackground" Value="#151d2e"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="False"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="EnableColumnVirtualization" Value="True"/>
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        </Style>

        <Style x:Key="TerminalCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TerminalRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#141b2b"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border x:Name="RowBorder" Background="{TemplateBinding Background}" BorderThickness="0,0,0,1" BorderBrush="#20283a">
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <SelectiveScrollingGrid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </SelectiveScrollingGrid.RowDefinitions>
                                <DataGridCellsPresenter Grid.Column="1" ItemsPanel="{TemplateBinding ItemsPanel}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                <DataGridDetailsPresenter Grid.Column="1" Grid.Row="1" SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" Visibility="{TemplateBinding DetailsVisibility}"/>
                                <DataGridRowHeader Grid.RowSpan="2" SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical" Visibility="Collapsed"/>
                            </SelectiveScrollingGrid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#12384a"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SignalRowStyle" TargetType="DataGridRow" BasedOn="{StaticResource TerminalRowStyle}">
            <Setter Property="Background" Value="#141b2b"/>
        </Style>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Cursor" Value="Arrow"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsOrderSubmitInFlight}" Value="True">
                        <Setter Property="Cursor" Value="Wait"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#070b12" BorderBrush="{StaticResource BorderBrushPanel}" BorderThickness="0,0,0,1" Padding="12,8">
            <DockPanel LastChildFill="False">
                <TextBlock Text="◈ TRADE TERMINAL" Foreground="{StaticResource CyanBrush}" FontSize="22" FontWeight="Bold" DockPanel.Dock="Left" Margin="0,0,22,0"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="Live Terminal" Foreground="{StaticResource TextPrimary}" Margin="0,0,18,0" FontWeight="SemiBold"/>
                    <!-- <TextBlock Text="Performance" Foreground="{StaticResource TextMuted}" Margin="0,0,18,0"/>
                    <TextBlock Text="Replay Engine" Foreground="{StaticResource TextMuted}"/> -->
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PanelStyle}" Background="#111827">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="◈ LIVE FEED" FontWeight="Bold" FontSize="20" Foreground="{StaticResource CyanBrush}"/>
                    <TextBlock Text="  ● " Margin="16,0,0,0" FontSize="18" Foreground="{Binding ConnectionStatusBrush}"/>
                    <TextBlock Text="{Binding ConnectionStatusText}" FontWeight="SemiBold" FontSize="15" Foreground="#94a3b8"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                    <TextBlock Text="Backend:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource TextMuted}"/>
                    <TextBox Width="500" Text="{Binding BackendBaseUrl, UpdateSourceTrigger=PropertyChanged}" Background="#0f172a" Foreground="{StaticResource TextPrimary}" BorderBrush="#334155" BorderThickness="1" Padding="8,5" FontFamily="Consolas"/>
                    <Button Content="Connect" Command="{Binding ConnectCommand}" Style="{StaticResource TerminalButton}" Margin="10,0,0,0"/>
                    <Button Content="Disconnect" Command="{Binding DisconnectCommand}" Style="{StaticResource TerminalButton}" Background="#3b1f31" Margin="6,0,0,0"/>
                    <Button Content="{Binding PauseResumeButtonText}" Command="{Binding PauseResumeCommand}" Style="{StaticResource TerminalButton}" Margin="6,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding ClockText}" FontSize="18" Foreground="#94a3b8" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PanelStyle}" Padding="6" Background="#0f172a">
            <ScrollViewer x:Name="TickerScrollViewer" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" Focusable="False">
                <ItemsControl ItemsSource="{Binding TickerItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="190" Margin="0,0,14,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="74"/>
                                    <ColumnDefinition Width="44"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Symbol}" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                <TextBlock Grid.Column="2" Text="{Binding PriceText}" Foreground="{Binding ChangeBrush}" TextAlignment="Right"/>
                                <TextBlock Grid.Column="3" Text="{Binding ChangePercentText}" Foreground="{Binding ChangeBrush}" TextAlignment="Left" Margin="5,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.95*"/>
                <ColumnDefinition Width="1.35*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="0.75*"/>
                <RowDefinition Height="0.75*"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="4" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="LIVE PRICES" Style="{StaticResource PanelTitle}"/>
                    <ListBox ItemsSource="{Binding PriceRows}" SelectedItem="{Binding SelectedPriceRow, Mode=TwoWay}" Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled" FontSize="16">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="1"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,2"/>
                                <Setter Property="Background" Value="{Binding RowBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{Binding ChangeBrush}"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="#20283a" BorderThickness="0,0,0,1">
                                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="8,0" VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.8*"/>
                                        <ColumnDefinition Width="1.15*"/>
                                        <ColumnDefinition Width="0.8*"/>
                                        <ColumnDefinition Width="0.7*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Symbol}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding BidAskText}" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding MidPriceText}" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding ChangePercentText}" Foreground="{Binding ChangeBrush}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="SIGNAL FEED" Style="{StaticResource PanelTitle}" Margin="0"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="Feed Count:" Foreground="{StaticResource TextMuted}" Margin="0,0,6,0"/>
                            <Border Background="#1d4ed8" CornerRadius="8" Padding="8,1" Margin="0,0,6,0">
                                <TextBlock Text="{Binding FeedCount}" Foreground="White" FontWeight="Bold"/>
                            </Border>
                            <Border Background="#083344" CornerRadius="8" Padding="8,1">
                                <TextBlock Text="{Binding ClientMsgPerSec, StringFormat={}{0} feeds/s}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                    <DataGrid ItemsSource="{Binding FeedRows}" Style="{StaticResource TerminalGridBase}" FontSize="14" RowHeight="32" CellStyle="{StaticResource TerminalCellStyle}">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource SignalRowStyle}">
                                <Setter Property="Background" Value="{Binding RowBackgroundBrush}"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding TimeText}" Width="0.8*"/>
                            <DataGridTextColumn Binding="{Binding Symbol}" Width="0.8*"/>
                            <DataGridTemplateColumn Width="0.8*">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding PriceText}" Foreground="{Binding PriceBrush}" FontWeight="Bold" VerticalAlignment="Center"/></DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Binding="{Binding BidAskFeedText}" Width="1.0*"/>
                            <DataGridTextColumn Binding="{Binding VolumeText}" Width="0.6*"/>
                            <DataGridTemplateColumn Width="0.6*">
                                <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding Direction}" Foreground="{Binding DirectionBrush}" FontWeight="Bold" VerticalAlignment="Center"/></DataTemplate></DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" Grid.Row="0" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ORDER ENTRY" Style="{StaticResource PanelTitle}" Margin="0"/>
                        <Button Grid.Column="1" Content="RESET DEMO" Command="{Binding ResetDemoCommand}" Style="{StaticResource TerminalButton}" Background="#451a03" Padding="8,3" FontSize="12"/>
                    </Grid>
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Button Content="{Binding BuyButtonText}" Command="{Binding SubmitBuyCommand}" Style="{StaticResource BuyOrderButton}"/>
                            <Border Background="#880a0e17" CornerRadius="2" Visibility="{Binding IsOrderSubmitInFlight, Converter={StaticResource BooleanToVisibilityConverter}}" IsHitTestVisible="False">
                                <TextBlock Text="SUBMITTING…" Foreground="{StaticResource YellowBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <UniformGrid Rows="2" Columns="2" Margin="0,0,0,10">
                            <TextBlock Text="MM Quote Limit:" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock Text="{Binding MarketMakerSummary.LimitText}" FontWeight="Bold"/>
                            <TextBlock Text="Status:" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock Text="{Binding MarketMakerSummary.StatusText}" FontWeight="Bold" Foreground="{StaticResource YellowBrush}"/>
                        </UniformGrid>
                        <Border Background="#0f172a" BorderBrush="{StaticResource BorderBrushPanel}" BorderThickness="1" CornerRadius="5" Padding="8">
                            <UniformGrid Rows="4" Columns="2">
                                <TextBlock Text="Position:" Foreground="{StaticResource TextMuted}"/>
                                <TextBlock Text="{Binding PositionSummary.QuantityText}" FontWeight="Bold"/>
                                <TextBlock Text="Avg Price:" Foreground="{StaticResource TextMuted}"/>
                                <TextBlock Text="{Binding PositionSummary.AveragePriceText}" FontWeight="Bold"/>
                                <TextBlock Text="Realized:" Foreground="{StaticResource TextMuted}"/>
                                <TextBlock Text="{Binding PositionSummary.RealizedPnlText}" FontWeight="Bold"/>
                                <TextBlock Text="Unrealized vs Mid:" Foreground="{StaticResource TextMuted}"/>
                                <TextBlock Text="{Binding PositionSummary.UnrealizedPnlText}" FontWeight="Bold"/>
                            </UniformGrid>
                        </Border>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Column="3" Grid.Row="0" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding DepthTitleText}" Style="{StaticResource PanelTitle}" Margin="0"/>
                        <TextBlock Grid.Column="1" Text="{Binding MatchingModelText}" Foreground="{StaticResource YellowBrush}" FontSize="11" FontWeight="Bold" Margin="8,0,18,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding AverageFillBadgeText}" Foreground="#7184a5" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                    </Grid>
                    <Border DockPanel.Dock="Top" Background="#312819" BorderBrush="#6f5517" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,8">
                        <TextBlock Text="{Binding ConsumedLiquidityText}" Foreground="{StaticResource YellowBrush}" FontSize="13" FontWeight="Bold" TextWrapping="Wrap"/>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" CanContentScroll="False">
                        <StackPanel>
                            <ItemsControl ItemsSource="{Binding AskDepthRows}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Background="{Binding RowBrush}" Margin="0,0,0,2" Height="24">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="42"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding PriceText}" Foreground="{Binding PriceBrush}" Margin="8,0" VerticalAlignment="Center" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding QuantityText}" Foreground="{Binding SideBrush}" Margin="8,0" VerticalAlignment="Center" FontWeight="Bold"/>
                                        <StackPanel Grid.Column="2" Margin="0,0,8,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding HitText}" Foreground="{StaticResource YellowBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Border Background="#0c1220" BorderBrush="{StaticResource BorderBrushPanel}" BorderThickness="1,1,1,1" CornerRadius="3" Padding="3" Margin="0,2">
                            <TextBlock Text="{Binding DepthMidText}" HorizontalAlignment="Center" Foreground="#7184a5" FontSize="13" FontWeight="Bold"/>
                        </Border>
                        <ItemsControl ItemsSource="{Binding BidDepthRows}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Background="{Binding RowBrush}" Margin="0,0,0,2" Height="24">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="42"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding PriceText}" Foreground="{Binding PriceBrush}" Margin="8,0" VerticalAlignment="Center" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding QuantityText}" Foreground="{Binding SideBrush}" Margin="8,0" VerticalAlignment="Center" FontWeight="Bold"/>
                                        <StackPanel Grid.Column="2" Margin="0,0,8,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding HitText}" Foreground="{StaticResource YellowBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="1" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="ORDER LIFECYCLE" Style="{StaticResource PanelTitle}"/>
                    <DataGrid x:Name="LifecycleGrid" ItemsSource="{Binding LifecycleRows}" Style="{StaticResource TerminalGridBase}" RowStyle="{StaticResource TerminalRowStyle}" FontSize="14" RowHeight="29" CellStyle="{StaticResource TerminalCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="8">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Width="3" Background="{Binding AccentBrush}" HorizontalAlignment="Left"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="128">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding TimeText}" Foreground="#7184a5" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="88">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Symbol}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="190">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Stage}" Foreground="{Binding StageBrush}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Message}" Foreground="#cbd5e1" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Grid.Row="2" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="SERVER METRICS" Style="{StaticResource PanelTitle}"/>
                    <UniformGrid Rows="3" Columns="4">
                        <StackPanel Margin="5"><TextBlock Text="PROCESSED" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding ProcessedCount}" Foreground="{StaticResource CyanBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="DROPPED" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding DroppedCount}" Foreground="{StaticResource RedBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="QUEUE" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding QueueDepth}" Foreground="{StaticResource CyanBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="RATE" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding ServerRate, StringFormat={}{0:0}}" Foreground="{StaticResource YellowBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="SNAPS" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding SnapshotsPerSec, StringFormat={}{0:0}}" Foreground="{StaticResource CyanBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="EVENTS" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding ServerGeneratedTotal}" Foreground="{StaticResource CyanBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="COAL/s" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding CoalescedPerSec, StringFormat={}{0:0}}" Foreground="{StaticResource GreenBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="CPU" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding CpuUsageText}" Foreground="{StaticResource CyanBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="MEM" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding MemoryUsageText}" Foreground="{StaticResource CyanBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="SET" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding WorkingSetText}" Foreground="{StaticResource CyanBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                        <StackPanel Margin="5"><TextBlock Text="THR" Foreground="{StaticResource TextMuted}" FontSize="11"/><TextBlock Text="{Binding ThreadCount}" Foreground="{StaticResource CyanBrush}" FontSize="20" FontWeight="Bold"/></StackPanel>
                    </UniformGrid>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Grid.Row="3" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="SERVER EVENT RATE (msg/s)" Style="{StaticResource PanelTitle}"/>
                    <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrushPanel}" BorderThickness="1" CornerRadius="3" Background="#111827">
                        <controls:RateGraphControl Samples="{Binding RateSamples}" CurrentRateText="{Binding CurrentRateText}"/>
                    </Border>
                    <Grid Grid.Row="2" Margin="0,4,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding RateAxisLabel0}" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                        <TextBlock Grid.Column="1" Text="{Binding RateAxisLabel1}" Foreground="{StaticResource TextMuted}" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding RateAxisLabel2}" Foreground="{StaticResource TextMuted}" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="{Binding RateAxisLabel3}" Foreground="{StaticResource TextMuted}" FontSize="11" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="Now" Foreground="{StaticResource TextMuted}" FontSize="11" HorizontalAlignment="Right"/>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="2" Grid.Row="2" Grid.RowSpan="2" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="TRADE MONITOR" Style="{StaticResource PanelTitle}"/>
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="56"/>
                            <ColumnDefinition Width="64"/>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="74"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ORDER ID" Foreground="#7184a5" FontSize="10" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="SYMBOL" Foreground="#7184a5" FontSize="10" FontWeight="Bold"/>
                        <TextBlock Grid.Column="2" Text="STATUS" Foreground="#7184a5" FontSize="10" FontWeight="Bold"/>
                        <TextBlock Grid.Column="3" Text="VENUE" Foreground="#7184a5" FontSize="10" FontWeight="Bold"/>
                        <TextBlock Grid.Column="4" Text="LATENCY" Foreground="#7184a5" FontSize="10" FontWeight="Bold"/>
                        <TextBlock Grid.Column="5" Text="FILL %" Foreground="#7184a5" FontSize="10" FontWeight="Bold"/>
                        <TextBlock Grid.Column="6" Text="PNL" Foreground="#7184a5" FontSize="10" FontWeight="Bold"/>
                    </Grid>
                    <ItemsControl ItemsSource="{Binding TradeMonitorRows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Background="{Binding RowBrush}" Margin="0,0,0,4" Height="24">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="48"/>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="56"/>
                                        <ColumnDefinition Width="64"/>
                                        <ColumnDefinition Width="48"/>
                                        <ColumnDefinition Width="74"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="3" Background="{Binding HealthBrush}" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding OrderIdText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Symbol}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Status}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Venue}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="4" Text="{Binding LatencyText}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="5" Text="{Binding FillText}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="6" Text="{Binding PnLText}" Foreground="{Binding PnLBrush}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </Border>

            <Border Grid.Column="3" Grid.Row="2" Grid.RowSpan="2" Style="{StaticResource PanelStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="PORTFOLIO" Style="{StaticResource PanelTitle}"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>
                            <UniformGrid Rows="1" Columns="3" Margin="0,0,0,8">
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,0">
                                    <StackPanel><TextBlock Text="Net Position" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding PortfolioStats.NetPositionText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel>
                                </Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,0">
                                    <StackPanel><TextBlock Text="Portfolio MTM" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding PortfolioStats.PortfolioMtmText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel>
                                </Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5">
                                    <StackPanel><TextBlock Text="Realized PnL" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding PortfolioStats.RealizedPnlText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel>
                                </Border>
                            </UniformGrid>
                            <TextBlock Text="SELECTED SYMBOL STATS" Style="{StaticResource PanelTitle}" FontSize="13" Margin="0,0,0,6"/>
                            <UniformGrid Columns="2">
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,5"><StackPanel><TextBlock Text="Orders Sent" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.OrdersSentText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,0,5"><StackPanel><TextBlock Text="Filled" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.OrdersFilledText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,5"><StackPanel><TextBlock Text="Rejected" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.RejectedText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,0,5"><StackPanel><TextBlock Text="Routed Fill Rate" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.FillRatioText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,5"><StackPanel><TextBlock Text="Cancels" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.CancelsText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,0,5"><StackPanel><TextBlock Text="Last Order" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.LastOrderText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,5"><StackPanel><TextBlock Text="Mark Mid" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.MarkMidText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,0,5"><StackPanel><TextBlock Text="Arrival" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.ArrivalText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,5"><StackPanel><TextBlock Text="Avg Fill" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.AverageFillText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,0,5"><StackPanel><TextBlock Text="Slippage" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.SlippageText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,5"><StackPanel><TextBlock Text="Total" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.TotalLatencyText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,0,5"><StackPanel><TextBlock Text="Risk" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.RiskLatencyText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,5"><StackPanel><TextBlock Text="Route" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.RouteLatencyText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,0,5"><StackPanel><TextBlock Text="Exchange" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.ExchangeLatencyText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5" Margin="0,0,5,0"><StackPanel><TextBlock Text="Fill" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.FillLatencyText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                                <Border Background="#111827" CornerRadius="3" Padding="5"><StackPanel><TextBlock Text="Overhead" Foreground="{StaticResource TextMuted}" FontSize="10"/><TextBlock Text="{Binding ExecutionStats.OtherLatencyText}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" FontSize="13"/></StackPanel></Border>
                            </UniformGrid>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
